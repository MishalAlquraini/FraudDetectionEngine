package com.fraud.User

import com.sun.net.httpserver.Request
import jakarta.inject.Named


@Named
class UserService(
    private val userRepository: UserRepository
){
   // fun registerUser(request: )
}
